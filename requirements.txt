# System Monitor Backend Dependencies
# Install with: pip install -r requirements.txt

psutil>=5.9.0          # System resource metrics (CPU, RAM, Network I/O)
websockets>=12.0       # WebSocket server for streaming data
GPUtil>=1.4.0          # NVIDIA GPU metrics (load, VRAM, temperature)
